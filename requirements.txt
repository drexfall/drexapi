# drexapi — FastAPI + event-sourcing stack (Django fully removed).
# Curated top-level dependencies; pip resolves transitive ones.

# --- web / API ---
fastapi==0.115.6
uvicorn==0.30.5
python-multipart==0.0.18
gunicorn==23.0.0

# --- event sourcing + persistence ---
eventsourcing==9.5.4
psycopg2-binary==2.9.12      # postgres event store (prod)
pymongo==4.17.0              # read models
dnspython==2.8.0             # mongo+srv URIs

# --- config / validation ---
pydantic==2.13.3
pydantic-settings==2.7.0
python-dotenv==1.2.2

# --- auth (Auth0) ---
PyJWT==2.12.1
requests==2.33.1

# --- async tasks (carousel render; BackgroundTasks today, Celery for scale) ---
celery==5.6.3
redis==7.4.0

# --- carousel render pipeline ---
Jinja2==3.1.6
weasyprint==62.3
pypdfium2==5.7.1
Pillow==11.0.0
imagekitio==5.4.0

# --- resume parser ---
pdfplumber==0.11.9
nltk==3.9.4
